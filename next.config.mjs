/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
      domains: ['images.unsplash.com', 'res.cloudinary.com', 'assets.aceternity.com', 'images.pexels.com']
  }
};

export default nextConfig;